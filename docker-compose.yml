services:
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_USER: n_dawg
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: economap
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
